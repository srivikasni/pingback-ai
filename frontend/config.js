// Change this to your deployed backend URL once you deploy to Render.
// While testing locally, keep it pointing at your local server.
const API_BASE_URL = 'http://localhost:5000/api/opportunities';
